var React = require('react');

var components = React.createClass ({
    render: function () {
        return (
          <h1> This is your basic react </h1>
        );
    }
});

module.exports = components;
